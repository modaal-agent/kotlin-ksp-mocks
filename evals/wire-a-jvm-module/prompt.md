---
description: A Kotlin/JVM module whose tests hand-write a fake asks for generated mocks.
tags: [wiring, jvm]
allowed_tools: [Read, Glob, Grep, Skill]
max_turns: 8
expected_outcome: |
  Three edits: the Maven host in settings.gradle.kts, the KSP plugin plus
  kspTest("dev.modaal:mocks-processor:<version>") in feature/build.gradle.kts, and
  com.example.feature.FeedEnvironment in ksp { arg("kspMocksTargets", …) }. The version is read
  from the host's maven-metadata.xml rather than written from memory. The generated file lands
  under feature/build/generated/ksp/test/kotlin/ and is not committed. No MockK, no Mockito.
---

Our `:feature` module is Kotlin/JVM, and its tests hand-write a fake for this interface:

```kotlin
package com.example.feature

interface FeedEnvironment {
  suspend fun load(id: String): FeedPage
  fun log(message: String)
}
```

`feature/build.gradle.kts` today:

```kotlin
plugins {
  kotlin("jvm")
}

dependencies {
  testImplementation(kotlin("test"))
}
```

I want that fake generated by the dev.modaal mocks-processor instead of hand-written. Give me
every edit to make, and tell me where the generated file lands and whether it is committed.
